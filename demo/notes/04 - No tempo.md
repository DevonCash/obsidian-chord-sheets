No `tempo` property at all, so this note must behave exactly as the plugin did
before: the playback bar shows the original 1-20 speed slider rather than BPM
and time-signature fields, and scrolling is constant-rate.

The metronome button should not offer tempo controls here either.

```chords
[Verse]
| Em      Am | C       G |
no tempo set, so the speed slider drives the scroll
| Em      Am | C       G |
and nothing here is beat-aware
```
